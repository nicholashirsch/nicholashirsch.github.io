import { defineConfig } from 'vite'

export default defineConfig({
  // GitHub Pages: if this deploys to <user>.github.io/portfolio/, uncomment.
  // Leave commented for a user site (<user>.github.io) or a custom domain.
  // base: '/portfolio/',
})
